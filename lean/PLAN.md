# Formalization plan — rank-$r$ partial-trace inequality

Target: `rank-r.tex`, "A Sharp Rank-$r$ Partial-Trace Inequality via Double-Skew
Singular-Value Bounds" (July 26, 2026).

## 0. Why this is worth doing, and what that implies about ordering

The manuscript's own disclosure says the arguments were generated by GPT-5.6, that
the author did not independently derive them, and that no expert has checked them.
Its central import, Fu–Gao–Park [arXiv:2607.21367], was posted **three days before**
the manuscript and is itself unreviewed.

So formalization here is not a victory lap over a known-good proof — it is a
**measuring instrument**. That changes the ordering rule:

> Do not work in order of the paper. Work in order of *where a wrong step would
> hide*, cheapest-to-check first.

Everything below is organized by **delicacy**, not by size. Size is not the
constraint: mechanical index-pushing is grindable and the tier tables say so
explicitly. The constraints are (a) steps where the mathematics may actually be
wrong, and (b) steps needing a theorem that does not exist in any Lean library,
which no amount of grinding produces.

---

## 1. Setup

```
lean/
  lean-toolchain      leanprover/lean4:v4.32.0
  lakefile.toml       mathlib @ v4.32.0, Physlib @ master
  RankR/
    Conventions.lean  §1 conventions + main statement + FGP interface (Milestone 0)
```

Build:

```bash
cd /home/elazarg/workspace/quantum/lean
lake update            # resolves mathlib + Physlib
lake exe cache get     # Mathlib oleans (do NOT skip; source build is hours)
lake build RankR
```

Physlib has no prebuilt cache, but Lake compiles only the transitive closure of
what you actually `import`. Importing `QuantumInfo.ForMathlib.Matrix` and
`QuantumInfo.Channels.Unbundled` pulls in a small fraction of its 40.6k lines —
not the entropy/Stein's-lemma stack.

### What the libraries already give us

**Mathlib v4.32:**

| Have | Name |
| --- | --- |
| Inner product space on $E \otimes F$ | `TensorProduct.instInnerProductSpace`, `mapIsometry`, `assocIsometry`, `OrthonormalBasis.tensorProduct` |
| Singular values (skeleton) | `LinearMap.singularValues` — nonneg, antitone, `support = range (finrank range)`. **No SVD, no Ky Fan, no $\sum s_i^2 = \lVert T\rVert_{HS}^2$** |
| PSD order | `Matrix.PosSemidef`, `Matrix.PosDef`, `Analysis/Matrix/Order.lean` |
| Spectral theorem, Rayleigh | `Matrix.IsHermitian.spectral_theorem`, `InnerProductSpace/Rayleigh.lean` |
| Kronecker product | `Matrix.kroneckerMap`, `⊗ₖ` |
| Frobenius **norm** | `Matrix.frobenius_norm_def` — scoped, non-instance; **no inner product space** |
| Exterior powers | `exteriorPower` — algebraic only, **no inner product** |

**Physlib `QuantumInfo`:**

| Have | Name |
| --- | --- |
| Partial trace | `Matrix.traceLeft` / `Matrix.traceRight` (+ `PosSemidef`, trace lemmas) |
| Choi / Kraus | `MatrixMap.choi_matrix`, `of_choi_matrix`, `choi_equiv`, `of_kraus`, `exists_kraus`, `choi_of_kraus` |
| Positivity of maps | `MatrixMap.IsPositive`, `IsCompletelyPositive` (closed under `comp`, `add`, `smul`, `kron`, `piProd`) |
| Tensor of maps | `MatrixMap.kron` (`⊗ₖₘ`) |
| Trace norm, Schatten (Hermitian) | `HermitianMat/Schatten.lean`, `MatrixNorm/TraceNorm.lean` |

**Neither library has:** Schmidt decomposition of a bipartite vector, Schmidt
rank, Schmidt number, partial transpose, block positivity, $r$-positivity,
vectorization, Ky Fan, Eckart–Young, SVD, Takagi factorization.

---

## 2. Dependency spine

```
FGP Thm 2.4  ─┐
Ky Fan (∑s²) ─┼─> Lem 2.1 (double-skew) ─┐
Eckart–Young ─┘                          ├─> Prop 2.2 (H_r ⪰ 0) ─> Thm 1.1
                     Λ_E is CP ──────────┘         │
                     Lem 3.1 (contractions) ───────┘
                                                    │
Schmidt rank/number + block positivity ─────────────┼─> §5 two-copy
                                                    ├─> §6 witnesses
                                                    └─> §7 higher-copy
Ky Fan duality ─────────────────────────────────────> Cor 8.1 (Kronecker sums)
```

---

## 3. Delicacy tiers

### D0 — Convention risk. *Mechanical to write, highest error density.*

The paper mixes conventions that are individually standard but jointly easy to
get wrong. All are already written down in `RankR/Conventions.lean`.

| Item | tex | Why it bites |
| --- | --- | --- |
| $\langle\cdot,\cdot\rangle$ conjugate-linear in **first** arg | §1 | Matches Mathlib; a flip swaps $\mathrm{Tr}\,C \leftrightarrow \overline{\mathrm{Tr}\,C}$ throughout §3 |
| $\mathrm{vec}(\lvert x\rangle\langle y\rvert) = x \otimes \bar y$ | §1 | Fixes whether `vec` is entrywise-identity or entrywise-conjugate |
| $\mathrm{Tr}_U$ = trace **over** $U$ | §1 | Thm 1.1 is symmetric so an error is invisible there — it surfaces only in Thm 5.9 (asymmetric) |
| $\mathfrak{so}$ uses $L^T = -L$, not $L^H = -L$ | §2 | The whole `eq:T-orthogonal` argument needs $K^T = K$ |
| The regrouping $(A_1A_2):(B_1B_2)$ | §5 | Every §5–§7 statement is *about* this cut |
| Euclidean vs. sup norm on `EuclideanSpace ℂ (m × n)` | — | Lean-specific: Mathlib's default `Pi` norm is sup; using it typechecks and is wrong |

**Do this first. It is nearly free and it is where the payoff density is highest.**

### D1 — Mechanical. *Grind it.*

Voluminous index-pushing with no conceptual risk. Delegate freely.

| Item | tex | Notes |
| --- | --- | --- |
| Lemma 3.1, four contraction identities | `lem:partial-trace-contractions` | The bulkiest routine block in the paper; pure index computation once `hsNormSq` and `traceLeft/Right` are in place |
| Rank monotonicity $r_0 \to r$ | `eq:rank-monotonicity` | Two lines |
| Trace–rank Cauchy–Schwarz $\lvert\mathrm{Tr}\,C\rvert^2 \le r_0\lVert C\rVert_2^2$ | §3 | Via $\mathrm{Tr}(PC)$, $\lVert P\rVert_2^2 = r_0$ |
| Sharpness example $C = P_r \otimes \lvert v\rangle\langle w\rvert$ | §1 | Pure computation; confirms constants land on the right sides |
| Prop 4.1, Lagrange-identity SOS | `prop:sos-trace-rank` | Complex Lagrange identity; genuinely easy |
| $\Lambda_E(X) = \mathrm{Tr}(X)I - X^T$ is CP | `eq:Lambda-kraus` | Choi $= I - F$, spectrum $\subseteq \{0,2\}$; Physlib's `choi_of_kraus` does most of it |
| Lemma 5.4, one-sided bound | `lem:one-sided-partial-trace` | Trace-norm duality + $\lVert\cdot\rVert_1 \le \sqrt r\lVert\cdot\rVert_2$; Physlib has trace norm |
| Prop 7.1, inclusion–exclusion | `prop:mfold-inclusion-exclusion` | Expand the tensor product over subsets |
| §5 optimizations over $z \in [0, r]$ | `prop:exact-q2-score`, `thm:exact-asymmetric-score` | One-variable affine/quadratic minimization once the sign of the $z$-coefficient is cased |

### D2 — Delicate and self-contained. *This is where a real error would be.*

No external input needed; the argument either works or it doesn't. **Rank-ordered
by my estimate of where a defect is most likely.**

1. **The constant chain `eq:T-by-vertices` → `eq:T-Cauchy` → `eq:T-operator-bound`.**
   Four constants must conspire exactly: the $1/\sqrt2$ from $\eta_{ij}$'s
   normalization, the vertex degree $r-1$, the $1/2$ from Lemma 2.1, and
   $\lVert K^{(ij)}\rVert_2^2 = 4\lVert c^{(ij)}\rVert_2^2$ (from
   $\langle L_\alpha, L_{\alpha'}\rangle = 2\delta$). The product must be
   *exactly* $(r-1)\lVert c\rVert_2^2$ — not $\le$ something larger, or Prop 2.2
   fails and with it the whole paper. It does check by hand
   ($\frac{r-1}{2}\cdot\frac12\cdot 4 = r-1$), but this is precisely the chain
   Lean verifies for free once the pieces are stated, and precisely the kind of
   thing an LLM gets right four times out of five.

2. **`eq:Phi-expansion`**, the four-term expansion of
   $\Phi = \Lambda_U \otimes \Lambda_V \otimes \mathrm{id}_Q$, and its
   specialization `eq:Phi-rhoT` at $Y = \rho^{T_{UV}}$. Signs, and which factor
   carries which partial transpose. The paper's own caveat — "operators are
   placed on their labeled tensor factors, regardless of whether those factors
   are adjacent in the displayed tensor ordering" — is exactly the informality
   Lean will not accept. **Budget real effort for explicit reindexing
   equivalences here**; this is the one place where formalizing costs
   substantially more than reading.

3. **$\Phi(P_-) = \mathcal{T}\mathcal{T}^*$** (`eq:Phi-Pminus-TTstar`). Justified
   by "the vectors in the first line are exactly the columns of $\mathcal T$
   indexed by $(i,j,\alpha,\beta)$". Needs an actual index bijection.

4. **`eq:sos-complete-graph`** (Prop 4.3) claims an **identity**, not a bound.
   The vertex-variance identity is true at degree $r-1$; the risk is the
   prefactor bookkeeping in "adding and subtracting the total edge action gives
   exactly".

5. **`eq:T-orthogonal`**, $\langle\psi, \mathcal Tc\rangle = 0$. Short but
   load-bearing: it is the only thing upgrading $\mathcal T\mathcal T^* \preceq
   (r-1)I$ to $\preceq (r-1)(I - \rho)$, and without the upgrade Thm 1.1 loses
   the $\frac1r\lvert\mathrm{Tr}\,C\rvert^2$ term.

6. **`eq:kronecker-CS`** (Cor 8.1). A Cauchy–Schwarz producing the specific
   combination $\lVert\mathrm{Tr}_1C\rVert_2^2 + \lVert\mathrm{Tr}_2C\rVert_2^2
   - \frac2d\lvert\mathrm{Tr}\,C\rvert^2$ — the $2/d$ comes from tracelessness
   and is easy to misplace.

7. **Thm 7.5 / Thm 7.6** (`thm:mfold-regimes`, `thm:mfold-diagonal-threshold`),
   the heaviest combinatorics in the paper. Sign analysis of the odd-subset
   polynomial $\vartheta_m(t)$. Lowest priority because nothing else depends on
   them, highest per-item risk.

### D3 — External index-matching. *Cheap to check, expensive to get wrong.*

**Lemma 2.1's application of FGP Thm 2.4.** FGP states
$\langle\psi\lvert(\Pi_{\mathcal A}^{(13)} \otimes \Pi_{\mathcal A}^{(24)})\rvert\psi\rangle \le 1/2$
for $SR(\psi) \le 2$ on $(\mathbb C^d)^{\otimes 2}_{12} \otimes (\mathbb C^d)^{\otimes 2}_{34}$,
i.e. antisymmetric pairs $(1,3)$ and $(2,4)$ with the Schmidt cut $12:34$. The
paper uses antisymmetric pairs $(U_{\rm out}, U_{\rm in})$, $(V_{\rm out}, V_{\rm in})$
with cut $(U_{\rm out}V_{\rm out}):(U_{\rm in}V_{\rm in})$. **These must be the same
statement under relabeling.** Also in scope: the zero-extension of $U, V$ into a
common $\mathbb C^d$ preserving nonzero singular values and landing in
$\mathfrak{so}(\mathbb C^d) \otimes \mathfrak{so}(\mathbb C^d)$.

If there is an error anywhere in this paper, my bet is here. **Check it
numerically before touching Lean** (see §5).

### D4 — Library-bottleneck. *Grinding does not help; the theorem must exist.*

| Needed | Status | Tractable? |
| --- | --- | --- |
| **Ky Fan max principle** for $\sum_{i\le k} s_i^2$ | Absent everywhere | Yes — standard, follows from Mathlib's spectral theorem + Rayleigh. **PR-worthy.** |
| **Best Schmidt-rank-$k$ approximation** (Johnston–Kribs Thm 3.3 = Eckart–Young in Schmidt-coefficient form), `eq:best-SR2-approximation` | Absent | Yes — standard. **PR-worthy.** |
| **Schmidt decomposition** of a bipartite vector | Absent from Mathlib *and* Physlib | Yes, given SVD. Prerequisite for everything in §5–§7. **PR-worthy.** |
| **SVD existence** for complex matrices | Mathlib has `singularValues` but not the factorization | Yes. **PR-worthy.** |
| $\sum_i s_i^2 = \lVert C\rVert_2^2$, Frobenius `InnerProductSpace` | Absent | Yes, easy |
| Schmidt number, block positivity, $r$-positivity + Choi correspondence | Absent | Yes — definitional work plus Physlib's Choi layer |
| Ky Fan **duality**, Frobenius-dual form `eq:ky-fan-duality` | Absent | Yes, given Ky Fan |
| **FGP Thm 2.4** — Takagi factorization (absent from Mathlib) + minimax + first/second variation | Absent | **No.** A separate research-scale project. |

**Decision: carry FGP as an explicit hypothesis, not an `axiom`.** State
`theorem rank_r_partial_trace (hFGP : DoubleSkewBound U V) : …`. Then
`#print axioms` stays a meaningful hygiene check, and the dependency on an
unreviewed three-day-old preprint is visible in the type rather than buried.

The five PR-worthy items are the reusable payoff: they are wanted by Mathlib
independently of this paper, and they are the only reason to touch Lean rather
than a CAS for the §5–§7 material.

---

## 4. Roadmap

Each phase ends in a checkable artifact. No `sorry` at a phase close;
`#print axioms` on the phase's theorem must show only `propext`,
`Classical.choice`, `Quot.sound` (plus the FGP hypothesis, never an axiom).

### Status

`RankR/` builds clean. **`rank_r_of_operatorIneq` — Theorem 1.1 given
Proposition 2.2 — is fully proved**, `#print axioms` showing only
`propext, Classical.choice, Quot.sound`.

| File | Content | `sorry` |
| --- | --- | --- |
| `Conventions.lean` | §1 conventions, `so(U)⊗so(V)`, the Fu–Gao–Park interface `DoubleSkewBound` | 0 |
| `HS.lean` | Hilbert–Schmidt layer, transported from `EuclideanSpace` (gap A) | 0 |
| `Elementary.lean` | `rank_mono`, the four contraction identities in Gram form, `normSq_trace_le`, sum reindexings | 0 |
| `Operator.lean` | `qform`, `placeUQ`/`placeVQ`, `margUQ`/`margVQ`, the contraction identities in operator form | 0 |
| `Factor.lean` | `exists_rankFactor` | 0 |
| `Main.lean` | `qform` algebra, `finrank = rank`, `HopScaled`, `OperatorIneq`, **Theorem 1.1** | 1 |

**The single `sorry` is `operatorIneq_of_doubleSkew`** — Proposition 2.2 from
the double-skew bound. That is tier D2, the manuscript's actual content.
Everything else in §§1–3 is machine-checked.

A structural note that shapes Phase A: the Lean `OperatorIneq` is deliberately
the *post-Schmidt normal form* of the paper's Prop 2.2 — `Q = Fin s` with `qᵢ`
the standard basis. That is all `rank_r_of_operatorIneq` consumes, so Phase A
needs no Schmidt decomposition. (The paper's general-`ψ` form with
`ρ_Q = I/r` would need it; nothing downstream does.)

### Phase A — close `operatorIneq_of_doubleSkew` (= old M3)

Highest value per effort: finishing it answers **"is the paper's own argument
correct, granted Lemma 2.1."** No missing library theorems stand in the way;
it is the same entrywise style as `Operator.lean`, roughly 3–4× the volume.
Each step ends compiling; each maps to a labeled equation.

- **A0 — Preflight extension.** Add `(dU, dV, 1)` triples to `check_Hr_psd`:
  the `r = 1` case is consumed by the assembly (rank-1 `C`), degenerates the
  complete-graph argument (no edges, `H = Φ(P₊)`), and is currently untested.
  (Checked ad hoc 2026-07-27: min eigenvalue $-7\times10^{-16}$ over 1000
  trials, five dimension pairs. Make it part of the suite.)
- **A1 — Skew groundwork.** Basis `L_ab = E_ab − E_ba`; membership of
  `∑ c_αβ L_α ⊗ₖ M_β` in `doubleSkew`; `Kᵀ = K` on `doubleSkew` by span
  induction from `(L⊗M)ᵀ = Lᵀ⊗Mᵀ = L⊗M`; conjugate of an orthonormal family
  is orthonormal. Mechanical.
- **A2 — `Φ` as an explicit Kraus sum**,
  `Φ(Y) = ∑_{α,β} (L_α⊗M_β⊗I) Y (L_α⊗M_β⊗I)ᴴ`, entrywise like `placeUQ`.
  **Skip the Choi/CP machinery entirely** — `Choi = I − F` is never needed:
  `qform (A Y Aᴴ) x = qform Y (Aᴴx)` makes PSD-preservation two lines. This
  drops the Physlib dependency for the phase (and retires the D1 table's
  "`Λ_E` is CP via Choi" item).
- **A3 — The sectors.** `η_ij`, `P₋`, `P₊`, and `eq:rho-partial-transpose` in
  unnormalized form: with `ρ₀ = |δ_e⟩⟨δ_e|` and `s = r` it reads
  `ρ₀^{T_UV} = P₊ − P₋` — denominator-free, continuing the `HopScaled`
  convention.
- **A4 — The decomposition** (`eq:H-decomposition`, scaled):
  `r·H = Φ(P₊) + (r−1)(I−ρ) − Φ(P₋)`. Entrywise algebra, preflight-exact.
  Grindable; delegate.
- **A5 — `Φ(P₋) = 𝒯𝒯*`** (`eq:Phi-Pminus-TTstar`, D2 #3). Define `𝒯` as a
  matrix with columns indexed by `(edge, α, β)` — fixing the column indexing
  the paper never states (MANUSCRIPT-NOTES §4). **Budget the most debugging
  time here.**
- **A6 — The bound `‖𝒯c‖² ≤ (r−1)‖c‖²`.** Design decision: **use §4's
  `eq:sos-complete-graph` identity, not §2's Cauchy–Schwarz chain
  (`eq:T-Cauchy`).** The SOS route turns the zero-slack constant chain (D2 #1)
  into: one *exact algebraic identity* (preflight-exact to $1.8\times10^{-15}$)
  + edgewise `DoubleSkewBound` on the bracket `½‖K‖² − ‖Kēᵢ‖² − ‖Kēⱼ‖²`
  + a manifestly nonnegative vertex-variance sum. Identities grind well in
  Lean; chained inequalities do not. Proves Prop 4.3 as a byproduct. Fallback:
  `eq:T-by-vertices` + per-vertex Cauchy–Schwarz.
- **A7 — `⟨δ_e, 𝒯c⟩ = 0`** (`eq:T-orthogonal`, D2 #5), from `Kᵀ = K`. Short.
- **A8 — Assembly.** `𝒯*ψ = 0` from A7, so
  `‖𝒯*x‖² = ‖𝒯*(x − ρx)‖² ≤ (r−1)(‖x‖² − |⟨ψ,x⟩|²)`; get
  `𝒯𝒯* ⪯ (r−1)I` square-root-free via
  `‖𝒯*x‖⁴ = ⟨𝒯(𝒯*x), x⟩² ≤ (r−1)‖𝒯*x‖²‖x‖²` and cancellation. Combine
  with A4 and `Φ(P₊) ⪰ 0`.

*Artifact: `rank_r_partial_trace (hFGP : DoubleSkewBound U V)` with no
`sorry`; `#print axioms` clean.*

### Phase B — shrink the trusted surface to FGP's literal statement (D3 + tractable D4)

After A, everything rests on `DoubleSkewBound` — Lemma 2.1 *as consumed*,
whose derivation is where this plan bets an error would hide. Phase B replaces
that trust with FGP Thm 2.4 as literally published. Split it: the risky part
is cheap, the expensive part is safe.

- **B1 — The index-matching (do first; no new library).** State `FGPBound d`
  verbatim: antisymmetric projectors on pairs (1,3), (2,4) of `(ℂ^d)⊗⁴`,
  Schmidt cut 12:34, overlap ≤ ½ for Schmidt rank ≤ 2. Prove the paper's
  crossed-cut application equal to it under an explicit `Equiv` of the four
  factors. **This is tier D3 itself — the top-risk step — and it needs only
  reindexing.** Use the concrete model `∧²U ≅ {L : Lᵀ = −L}` with the HS
  inner product; avoid Mathlib's inner-product-less `exteriorPower`.
- **B2 — Zero-extension.** Coordinate-subspace inclusion preserves skewness
  and nonzero singular values. Small.
- **B3 — The two analytic links.** Ky Fan for `k = 2` in the easy direction
  (`‖Kx‖² + ‖Ky‖² ≤ s₁² + s₂²`, spectral theorem on `KᴴK`; do not build
  general Ky Fan), and the best-Schmidt-rank-2 identity
  `eq:best-SR2-approximation`, which needs **complex SVD existence — the one
  big rock**. SVD, Ky-Fan-2, and `∑sᵢ² = ‖·‖₂²` go in a `ForMathlib/`
  subdirectory for upstreaming.

*Artifact: `rank_r_partial_trace (hFGP : FGPBound d)` — trusted surface
exactly FGP's published statement. A legitimate stopping point.*

### Phases C–F — manuscript coverage, ordered by payoff/cost

- **C — Finish §4** (cheap after A6): Prop 4.1's Lagrange SOS (D1), Prop 4.2's
  sector Gram identity. Rounds out "§§1–4 fully checked".
- **D — §5 two-copy** (after B3, which supplies SVD): partial transpose
  (entrywise definition), Schmidt rank of a bipartite vector, block
  positivity; then `prop:exact-q2-score` and `thm:exact-asymmetric-score`
  (one-variable minimization, D1). The asymmetric theorem is where the
  `Tr_U` convention is finally exercised (D0).
- **E — §6 + §8**: Schmidt number (infimum over decompositions), trace-norm
  separation (Physlib's trace norm finally earns its import), Ky Fan duality
  for Cor 8.1 (D2 #6).
- **F — §7 higher-copy**: last — nothing depends on it — but note it has the
  *highest unique value per theorem*: `thm:mfold-diagonal-threshold` and the
  `ϑ_m(t)` sign analysis (D2 #7) are the only major claims the preflight
  could not test at all, so Lean is the first check they would ever get.

### Ordering rationale and stop points

**A then B1, before anything else.** A closes the paper's own argument; B1
attacks the one step most likely to hide a real error, and it is cheap.
Everything after that is coverage, not risk reduction. Stopping after A
verifies the theorem modulo Lemma 2.1; stopping after B modulo FGP's
published statement.

### Delegation

A1–A4 and C are grindable — hand to the `lean4` sorry-filler/proof-repair
agents. Keep A5, A6, and all of B1 in the main loop: they are where fidelity
to the paper is the point, not proof search.

---

## 5. Pre-flight — DONE, 19/19 pass

`../preflight.py`. Every check tests a specific labelled equation at small
dimensions. **All 19 pass.** Notable:

| Check | Result |
| --- | --- |
| Thm 1.1 (`eq:main-bound`) | holds; min normalized slack $9.3\times10^{-4}$ |
| §1 sharpness — all four claimed quantities *and* equality | exact |
| **Lem 2.1** $s_1^2+s_2^2 \le \frac12\lVert K\rVert_2^2$ | holds; ratio hits **exactly $0.5$** at $2{\times}2, 2{\times}3, 3{\times}2, 3{\times}3$ |
| $K^T = K$ on $\mathfrak{so}\otimes\mathfrak{so}$ | exact |
| `eq:Lambda-kraus`, Choi $= I-F$, spectrum $\{0,2\}$ | exact |
| **`eq:Phi-expansion`** (D2 #2) via Kraus vs. the 4-term formula | exact |
| `eq:rho-partial-transpose` $\rho^{T_{UV}} = \frac1r(P_+-P_-)$ | exact |
| **`eq:T-operator-bound`** (D2 #1, the constant chain) | holds; **attained exactly**, ratio $= 1.000000$ |
| `eq:T-orthogonal` | $\lvert\langle\psi,\mathcal Tc\rangle\rvert < 10^{-15}$ |
| `eq:Phi-Pminus-TTstar`, `eq:Kc-norm`, `eq:T-by-vertices` | exact |
| **Prop 2.2** $H_r \succeq 0$ | holds; min eigenvalue $-4.6\times10^{-16}$ |
| `eq:H-decomposition` | exact |
| **`eq:sos-complete-graph`** as an *identity* (D2 #4) | exact, rel. error $1.8\times10^{-15}$ |
| `prop:exact-q2-score`, `thm:exact-asymmetric-score` | formula matches, both branches attained |
| `prop:mfold-inclusion-exclusion` | exact |
| `thm:mfold-regimes` (1) and (2) | thresholds correct in both directions |
| `cor:kronecker` | holds |

**Two findings that matter for the Lean work:**

1. **The D2 #1 constant chain is tight, ratio exactly $1.000000$.** There is no
   slack absorbing an arithmetic slip: at $\dim U = \dim V = 2$, $\mathfrak{so}(2)
   \otimes \mathfrak{so}(2)$ is one-dimensional, so $K$ has all singular values
   equal and *every* orthonormal pair saturates Lemma 2.1. Any error in the
   $\frac1{\sqrt2}$ / $(r-1)$ / $\frac12$ / $4$ bookkeeping would have shown up
   here. It didn't. Good news for the paper; also means Lean must reproduce the
   constants exactly, with no room to be sloppy and still close the proof.

2. **Lemma 2.1 is verified as a *statement*, not as a *derivation*.** The test
   confirms $s_1^2+s_2^2 \le \frac12\lVert K\rVert_2^2$ holds numerically. It
   does **not** confirm that the paper's route to it — zero-extension, the
   $(\wedge^2U)\otimes(\wedge^2V)$ identification, best-rank-2 approximation,
   and the index-matching to FGP's $(1,3),(2,4)$ labeling with cut $12{:}34$ —
   is valid. Tier D3 remains open. A wrong derivation of a true statement is
   still a wrong paper, and this is the one place a numerical check cannot help.

**What the pre-flight cannot cover**, and therefore what Lean is actually for:

- Anything genuinely $r$-dependent. All checks run at $r \le 6$, $d \le 5$; an
  argument that fails only for large $r$ would pass here.  At the other end,
  `check_Hr_psd` starts at $r = 2$, while the Lean assembly also consumes
  $r = 1$ — closing that gap is Phase A0.
- Tier D3 (above).
- §6: `thm:pcp-rank-verifier`, `cor:two-copy-trace-distance`,
  `cor:exact-schmidt-number` — all quantify over states of bounded *Schmidt
  number*, which is not numerically checkable by sampling (it is an infimum over
  decompositions).
- `thm:mfold-diagonal-threshold` and the $\vartheta_m(t)$ sign analysis (D2 #7);
  only items (1) and (2) of `thm:mfold-regimes` were tested.

**Revised risk assessment.** The paper's algebra survives every test it is
possible to run cheaply, including both steps I ranked most likely to be wrong.
That materially raises the prior that M1–M3 will close. The residual risk is
concentrated in (a) tier D3, and (b) the §6/§7 material that sampling cannot
reach — which is also the material that needs the most missing library
infrastructure. Re-prioritize accordingly: **D3 is now the single most valuable
thing to attack**, and it must be attacked by reading FGP, not by computing.

---

## 6. Ground rules

- **Statements before proofs.** Every milestone starts by writing all its
  statements with `sorry`, then filling them. Statements are where the value is;
  proofs are where the grinding is.
- **FGP is a hypothesis, never an `axiom`.** Same for anything else imported
  from an unverified source.
- **`Matrix`, not abstract `TensorProduct`.** The paper fixes bases anyway
  (transposition, $\mathfrak{so}$, vectorization), Physlib's partial trace is
  matrix-level, and `Matrix (U × V) (U × V) ℂ` reindexes cheaply. Mathlib's
  `TensorProduct.instInnerProductSpace` stays available if an abstract argument
  turns out to be cleaner.
- **Reindexing is not free.** The paper's "operators are placed on their labeled
  tensor factors" needs explicit `Equiv`s. Build a small reindexing API early
  (M0/M1) rather than fighting it inside D2 proofs.
- **Record every discrepancy**, including ones that turn out to be harmless
  typos. The output of this project is a list of what the paper got wrong, not
  just a `.olean`.
